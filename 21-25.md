เกรซจะไม่แยก process ของแต่ละข้อ เพราะมันเป็นการทำแบบต่อเนื่อง โอเค มาเริ่มกันได้

ขอแจ้งไว้ให้ทราบก่อน ว่าเกรซใช้ VM ของ Cisco ที่อยู่ในคอร์สเรียน และใช้แบบ Windows และเกรซไม่สามารถแคปภาพจากใน VM ได้ จึงใช้การถ่ายภาพผ่านโทรศัพท์

# Step 1 เปิด Burp Suite

ให้เราทำการเปิด Burp Suite ที่อยู่ใน Kali Linux ขึ้นมา จากนั้นไปที่แท็บ Proxy -> Intercept แล้วเปิดให้เป็น Intercept is on

----------------------------------------------------

ภาพที่ 1

# Step 2 เปลี่ยนเป็น Low

เปิดเว็บนี้ที่โจทย์ให้มา

```
http://10.6.6.13/vulnerabilities/sqli/
```

ทำการ login และใส่ password ให้เรียบร้อยตามทที่โจทย์ให้มา จากนั้นไปที่ DVWA Security แล้วเปลี่ยนจาก impossible เป็น Low -> กด Submit หลังจากนั้นดูตรง "Security level is currently: ..." ว่าเป็น low ถ้าเป็น low ต้องขึ้นแบบนี้ "Security level is currently: low."
